{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/pvp-leaderboard run build && cp -r artifacts/pvp-leaderboard/dist/public ./public",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    },
    {
      "source": "/((?!assets|favicon).*)",
      "destination": "/index.html"
    }
  ]
}
