{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/pvp-leaderboard run build",
  "installCommand": "pnpm install",
  "outputDirectory": "artifacts/pvp-leaderboard/dist",
  "rewrites": [
    {
      "source": "/((?!assets|favicon\\.svg|opengraph).*)",
      "destination": "/index.html"
    }
  ]
}
